spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mnriem/spring-microservices-performance-config.git
        native:
          search-locations: classpath:.
  profiles:
    active: native
